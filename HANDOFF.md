# DIALOGUE VERIFICATION MODEL — Claude引継ぎ

## 目的

このローカルWebサイトは、閉鎖的なオンライン会話で起こり得る情報経路の限定、選択的な称賛、異論の扱い、話題転換などを、**すべて架空のBOT会話**として観察する研究プロトタイプです。

実在の人物、組織、コミュニティ、会話を診断・評価・告発するためのサイトではありません。単発の発言から意図を断定せず、反応の偏り・異論への応答・根拠の比較可能性・情報経路の複数性が反復して現れるかを観察します。

## 起動

プロジェクト直下で実行します。

```powershell
npm install
npm run dev
```

ローカルプレビューは通常 `http://localhost:3001/` です。`npm run build` は成功しています。

## 主なファイル

- `app/page.tsx` — 圧力側の会話データ (`buildRun`)、モード切替、チャットの自動送出、観察パネル、理論セクション
- `app/data/types.ts` — `Message` / `Mode` / 検証基準の型
- `app/data/common-run.ts` — 共通前半（中間型）の会話と分岐点
- `app/data/open-run.ts` — 非圧力側後半の会話
- `app/data/criteria.ts` — 検証基準4項目の定義と、メッセージごとの寄与
- `app/data/verify.ts` — 検証モードの3局面・選択肢・結果文言
- `app/globals.css` — Apple風の淡いUI、スマホ幅チャット、観察パネル、理論カード、モードタブ、検証UI
- `app/layout.tsx` — タイトルと説明メタデータ

## モード構成（分岐モデル）

中心人物のいないコミュニティは中間型から始まり、そこから圧力型か非圧力型に分かれやすい、という仮説を構造にしたものです。3つは並列の標本ではなく、**共通の起点を持つ分岐**です。

| タブ | 中身 |
|---|---|
| 中間型 | 共通前半のみを無限に周回。分岐となる発言の直後に分岐マーカーが出る |
| 圧力側 | 共通前半 + 既存 `buildRun` の会話（2件目以降） |
| 非圧力側 | 共通前半 + `buildOpen` |
| 検証モード | 共通前半を流し、3局面で選択を求める |

観察用の3モードは自動送出のみで止まりません。前半は3タブとも同一です。一度前半を最後まで見ると、以降のタブ切り替えでは前半を表示済みとして即座に積み、分岐点から再開します。検証モードだけは選択の順序が要るため、常に前半から流します。

### 分岐の筋

共通前半では、異論の扱い（A）と称賛の非対称（C）が決着しないまま散発的に混ざります。誰も悪くありません。それが疲労として溜まった結果、最後に「まとめ役を決めませんか」という善意の提案（B）が出ます。ここが分岐点です。

- 圧力側 — まとめ役が経路になり、異論が再定義され、反応の偏りが固定化する
- 非圧力側 — まとめを作りつつ一次資料を併記し、保留された論点が場に戻る

分かれるのは提案の善悪ではなく、**その後の場の応答**です。非圧力側も「正解の場」としては描かず、手間が増えることを本文に含めています。

## 検証基準パネル（PATTERN SIGNAL）

以前の 0〜100 のリスクスコアは廃止しました。発言数を数えているだけで根拠が薄く、「単発の発言や反応の数で判定しない」というサイト自身の原則と衝突するためです。

現在は検証基準4項目の充足状態を表示します。

1. 根拠と反証が提示されるか
2. 発言者の資格ではなく内容が評価されるか
3. 異論を同じ場で比較できるか
4. 情報経路が複数あるか

状態は `— 未充足 / ◐ 部分 / ○ 充足` の3段階。表示済みメッセージの寄与を順に適用して決まります。中間型は◐が並んだまま止まり、圧力側は—に落ち、非圧力側は○が埋まります。同じ物差しで3モードを比較できます。

圧力側の会話（既存 `buildRun`）は書き換えていないため、寄与は `criteria.ts` の `PRESSURE_SHIFTS` がメッセージIDの接尾辞から引いています。

## 検証モード

共通前半を流し、3つの局面（異論が出た直後 / 反応の偏りが生じた直後 / まとめ役案が出た直後）で停止して選択肢を出します。選ぶとBOTが反応し、基準が動きます。

**1回の選択では基準は◐までしか動きません。** 同じ方向の選択が2回以上続いて初めて確定します。「反復して現れるかを観察する」という前提を体験に落としたものです。

結果に出すのは話者の類型判定ではなく、**場の応答の記述**です（「この場は集約側に着地しました」等）。どちらにも寄らず曖昧なまま終わる結果も用意しています。自由記述の入力とその判定は、サイトの前提と衝突するため採用していません。

## 自己点検 `/self`（別入口）

架空の会話ではなく、**回答者が関わっている実在の場**を同じ4基準で見るための別ページです。トップページの理論セクションの下に入口カードがあります。

場面を8つ提示し、`よくある / たまにある / ない / わからない` を選ぶだけです。思い出して記述させません。`たまにある` は単発なので確定させず、`よくある` が揃って初めて基準が振れます。

**前提との衝突を避けるための線**: 診断するのは場ではなく、**回答者から今どう見えているか**です。結果の文言はすべて「あなたには〜に見えています」で統一し、「このコミュニティは〜です」とは書きません。自由記述・組織名の入力は受け取らず、回答はブラウザの中だけで処理して送信も保存もしません。この線を崩す編集をしないでください。

- `app/data/self-check.ts` — 設計・重み・判定・結果文言
- `app/self/page.tsx` — 画面
- 設計記録: `docs/superpowers/specs/2026-09-04-self-check-design.md`

## BOTと会話の扱い

登場するBOT名・会話・資料・イベント・URLはすべて架空です。実在の会話の引用、人物名、組織名、画像、スクリーンショットは使用しません。

現在の主な役割:

- `圧力高め型BOT`: 情報経路の限定、論点の再定義、責任の個人化、外部言論の無効化、仲介役の強調などの架空パターン（圧力側のみ）
- `共感BOT`: 圧力高め型BOTの直後にのみ称賛を返し、選択的な反応を可視化する架空パターン（圧力側のみ）
- `問題提起型BOT` / `質問型BOT`: 根拠、反証、比較可能性を尋ねる役
- `情報共有型BOT`: 公開情報を比較・確認する材料を出す役
- `参加者`: 情報を自分で確認し、改善案を出す役
- `普通型BOT`: 中間型で場を動かす役。誰も中心ではない
- `AIニュースBOT`: 公開情報の入口となる短い定時要約の架空例
- `外部告知BOT`: 別経路の存在を示す架空例

### 編集時の重要条件

1. `圧力高め型BOT` はこのシミュレーション内の役名であり、現実の人物の意図を示す表現にしない。
2. POINTの説明は「〜の場合があります」「〜として読めます」など、構造上の仮説として書く。
3. 有益情報を出す側にも、限界・一次資料・改善可能性を含める。非圧力側を正解として描かない。
4. 実在のチェーン、SNSアカウント、URL、プロジェクト名、発言、画像を混ぜない。
5. 左側の会話は領域中立の語彙を維持する。現在は `参加者 / 公開情報 / システム変更 / 相談先` を基本語としている。
6. 中間型の話者に悪意を持たせない。曖昧さは中心人物の不在から生じる。

## 参考文献

UIにも外部リンクとして掲載しています。

1. Heycke et al. (2018), *The Expression and Transfer of Valence Associated with Social Conformity* — https://pmc.ncbi.nlm.nih.gov/articles/PMC6377616/
2. Curşeu et al. (2017), *Minority Dissent and Social Acceptance in Collaborative Learning Groups* — https://pmc.ncbi.nlm.nih.gov/articles/PMC5368259/
3. Heerdink et al. (2015), *Emotional reactions to deviance in groups* — https://pmc.ncbi.nlm.nih.gov/articles/PMC4466438/

これらは対話構造を見る理論的な着眼点であり、実在の個人・組織への診断根拠ではありません。

## 既知の変更点・未確定

- 観察用の3モード（中間型 / 圧力側 / 非圧力側）は末尾で seed を進めて無限に流れ続けます。停止するのは検証モードの選択待ちだけです。
- 分岐点のマーカーは、分岐となる発言の直後にインラインで表示されます。周回ごとに出ます。中間型では `圧力側を見る` / `非圧力側を見る` のボタンが付きます。
- タブの語彙（`圧力側 / 非圧力側`）は未確定です。`経路が集約する場 / 経路が分散する場` のような構造語に寄せる案があります。文言差し替えのみで変更できます。

## 次の編集で確認すること

- `npm run build` が通ること
- 4タブが切り替わり、各モードが自動送出されること
- 前半が2回以上ゆっくり再生されないこと
- 中間型が分岐点で終端し、分岐マーカーが見えること
- 右パネルの4基準がモードごとに異なる状態に着地すること
- 検証モードが3局面で停止し、単発の選択では基準が確定しないこと
- スマホ風チャットが幅430px・高さ960pxを維持すること
- チャット内に説明カードを増やさず、POINTと右側パネルの対応が分かること
- 理論セクションの外部リンクが残ること

## 設計記録

- `docs/superpowers/specs/2026-09-04-scenario-branching-design.md` — 分岐モデル
- `docs/superpowers/specs/2026-09-04-self-check-design.md` — 自己点検
